version: "3.8"
services:
  gameapptestauto:
    image: domdiej/domautogametest
    build: .
    container_name: gametestautoapp01
    ports:
      - "80:3000"
    environment:
      - PORT=3000
    volumes:
      - ./:/app 
    command: ["node", "app.js"]
    
